console.log('hello');

describe('service-worker-prefetch-example', () => {
    beforeEach(() => cy.visit('/'));
    it('should display the page', () => {
        console.log('fill this in');
    });
});
