console.log('hello');

describe('web-worker-multiple-workers-example', () => {
    beforeEach(() => cy.visit('/'));
    it('should display the page', () => {
        console.log('fill this in');
    });
});
