console.log('hello');

describe('service-worker-cross-tab-messaging-example', () => {
    beforeEach(() => cy.visit('/'));
    it('should display the page', () => {
        console.log('fill this in');
    });
});
